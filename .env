# ==============================================================================
# BANCO DE DADOS - PostgreSQL
# ==============================================================================
POSTGRES_USER=peertube
POSTGRES_PASSWORD=peertube_strong_pass_123
POSTGRES_DB=peertube

PEERTUBE_DB_USERNAME=peertube
PEERTUBE_DB_PASSWORD=peertube_strong_pass_123
PEERTUBE_DB_HOSTNAME=postgres
PEERTUBE_DB_NAME=peertube
PEERTUBE_DB_SSL=false

# ==============================================================================
# CONFIGURAÇÃO DO SERVIDOR PEERTUBE
# ==============================================================================
# Para uso local no Windows (adicione ao arquivo hosts: 127.0.0.1 peertube.localhost)
PEERTUBE_WEBSERVER_HOSTNAME=peertube.localhost
PEERTUBE_WEBSERVER_PORT=9000
PEERTUBE_WEBSERVER_HTTPS=false

# Chave secreta — gere a sua com: openssl rand -hex 32
# (no Windows PowerShell: -join ((1..32) | % { '{0:x2}' -f (Get-Random -Max 256) }))
PEERTUBE_SECRET=a1b2c3d4e5f6a1b2c3d4e5f6a1b2c3d4e5f6a1b2c3d4e5f6a1b2c3d4e5f6a1b2

# Trust proxy (rede interna do Docker)
PEERTUBE_TRUST_PROXY=["127.0.0.1", "loopback", "172.18.0.0/16"]

# ==============================================================================
# E-MAIL (desabilitado para uso local)
# ==============================================================================
PEERTUBE_SMTP_HOSTNAME=localhost
PEERTUBE_SMTP_PORT=25
PEERTUBE_SMTP_FROM=noreply@peertube.localhost
PEERTUBE_SMTP_TLS=false
PEERTUBE_SMTP_DISABLE_STARTTLS=true
PEERTUBE_ADMIN_EMAIL=admin@peertube.localhost
